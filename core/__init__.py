"""Stock agent core package.

Deterministic pipeline: ingest -> features -> screen -> analyze -> validate.
The LLM layer (optional) only narrates; it never computes signals.
"""

__version__ = "0.1.0"
