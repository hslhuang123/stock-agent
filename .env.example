# Copy to .env and fill in to enable the LLM analyst layer.
# Any OpenAI-compatible endpoint works (OpenAI, Azure, Groq, Together, local vLLM/Ollama).

OPENAI_API_KEY=
OPENAI_BASE_URL=
AGENT_MODEL=gpt-4o-mini
